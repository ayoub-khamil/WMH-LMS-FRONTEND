# Copy to .env for local development against `dotnet run` (port 5000).
VITE_API_BASE_URL=http://localhost:5000/api
# For the local production run, create .env.production.local (gitignored) with
# VITE_API_BASE_URL=http://localhost:8080/api so `vite build` targets the
# docker compose prod stack.
